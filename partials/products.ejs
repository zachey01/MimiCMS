<div class="container">
  <div class="row">
    <% products.forEach(product => { %>
    <div class="col-md-4">
      <div class="card mb-4 shadow-sm">
        <div class="card-body">
          <h4><%= product.name %></h4>
          <code>$<%= product.price %></с>
          <div class="d-flex justify-content-between align-items-center">
            <a href="#" class="btn btn-sm btn-outline-secondary">Buy</a>
          </div>
        </div>
      </div>
    </div>
    <% }); %>
  </div>
</div>
